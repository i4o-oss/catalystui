import { Button, Dropdown } from '@i4o-oss/catalystui'
import DemoCard from '../../components/demo-card'

# Dropdown

export function DropdownMenu() {
    const dropdownItems = [
		{
			label: 'Item 1',
		},
	    {
			label: 'Item 2',
	    },
	    {
			label: 'Item 3',
	    },
    ]

    return (
        <DemoCard>
    	    <Dropdown align='start' items={dropdownItems} trigger={<Button>Dropdown Menu</Button>} />
        </DemoCard>
    )

}

<DropdownMenu />
