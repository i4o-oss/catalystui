import { Toast } from '@i4o-oss/catalystui'
import DemoCard from '../../components/demo-card'

# Toast

<DemoCard>
	<Toast
		triggerLabel='Show Toast'
		title='You have chosen Chikorita'
		description='This is your starter pokemon. Take good care of it.'
		duration={5000}
		action='Okay'
		actionAltText='Accept action'
		dismiss={false}
	/>
</DemoCard>

### Example

```tsx
import { Button, Toast } from '@i4o-oss/catalystui'

export default () => {
	return (
		<Toast
			action={<Button>Okay</Button>}
			description='This is your starter pokemon. Take good care of it.'
			duration={5000}
			trigger={<span>Trigger</span>}
			title='You have chosen Chikorita'
		/>
	)
}
```

### Props

| Name         | Type                  | Default | Description                                |
| :----------- | :-------------------- | :------ | :----------------------------------------- |
| triggerLabel | `ReactNode \| string` | `null`  | The element that triggers the toast.       |
| title        | `string`              | `null`  | The title of the toast.                    |
| description  | `string`              | `null`  | The description of the toast.              |
| duration     | `number`              | `5000`  | The duration of the toast in milliseconds. |
| action       | `ReactNode`           | `null`  | The action button of the toast.            |

// TODO: Add examples for triggering the toast from a button click and from a function call

// TODO: Update action and dismiss styles in toast
