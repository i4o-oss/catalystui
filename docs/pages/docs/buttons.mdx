import {
	Button,
	PrimaryButton,
	DangerButton,
	IconButton,
} from '@i4o-oss/catalystui'
import { HomeIcon } from '@radix-ui/react-icons'
import DemoCard from '../../components/demo-card'
import '@i4o-oss/catalystui/main.css'

# Buttons

## Default

<DemoCard>
	<Button>Button</Button>
</DemoCard>

### Example

```tsx
import { Button } from '@i4o-oss/catalystui'

export default () => {
	return <Button>Button</Button>
}
```

### Props

| Name         | Type                        | Default | Description           |
| :----------- | :-------------------------- | :------ | :-------------------- |
| bg           | string                      |         | Background color      |
| borderRadius | string                      |         | Border radius         |
| children     | ReactNode                   |         | Button content        |
| className    | string                      |         | Additional class name |
| disabled     | boolean                     |         | Disabled state        |
| loading      | boolean                     |         | Loading state         |
| loadingText  | string                      |         | Loading text          |
| onClick      | (event: MouseEvent) => void |         | Click handler         |
| onMouseDown  | (event: MouseEvent) => void |         | Mouse down handler    |
| padding      | string                      |         | Padding               |
| leftIcon     | ReactNode                   |         | Left icon             |
| rightIcon    | ReactNode                   |         | Right icon            |
| shadow       | string                      |         | Box shadow            |
| textColor    | string                      |         | Text color            |
| textSize     | string                      |         | Text size             |
| tooltip      | string                      |         | Tooltip text          |
| type         | string                      |         | Button type           |

## Primary

<DemoCard>
	<PrimaryButton>Primary Button</PrimaryButton>
</DemoCard>

### Example

```tsx
import { PrimaryButton } from '@i4o-oss/catalystui'

export default () => {
	return <PrimaryButton>Primary Button</PrimaryButton>
}
```

### Props

Refer to [Default Button Props](#props).

## Danger

<DemoCard>
	<DangerButton>Danger Button</DangerButton>
</DemoCard>

### Example

```tsx
import { DangerButton } from '@i4o-oss/catalystui'

export default () => {
	return <DangerButton>Danger Button</DangerButton>
}
```

### Props

Refer to [Default Button Props](#props).

## Icon

<DemoCard>
	<IconButton icon={<HomeIcon />} />
</DemoCard>

### Example

```tsx
import { IconButton } from '@i4o-oss/catalystui'
import { HomeIcon } from '@radix-ui/react-icons'

export default () => {
	return <IconButton icon={<HomeIcon />} />
}
```

### Props

Refer to [Default Button Props](#props). Icon Button also has the following props:

| Name | Type      | Default | Description |
| :--- | :-------- | :------ | :---------- |
| icon | ReactNode |         | Icon        |

// TODO: Add variations for each button type, e.g. disabled, loading, etc.
