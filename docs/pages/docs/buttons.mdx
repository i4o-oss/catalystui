import {
	Button,
	PrimaryButton,
	DangerButton,
	IconButton,
} from '@i4o-oss/catalystui'
import { HomeIcon } from '@radix-ui/react-icons'
import DemoCard from '../../components/demo-card'
import '@i4o-oss/catalystui/main.css'

# Buttons

### Default

<DemoCard>
	<Button>Button</Button>
</DemoCard>

### Primary

<DemoCard>
	<PrimaryButton>Primary Button</PrimaryButton>
</DemoCard>

### Danger

<DemoCard>
	<DangerButton>Danger Button</DangerButton>
</DemoCard>

### Icon

<DemoCard>
	<IconButton icon={<HomeIcon />} />
</DemoCard>

// TODO: Add variations for each button type, e.g. disabled, loading, etc.

// TODO: Add example code and props table for each button type
